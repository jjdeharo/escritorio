Prompt para otra IA (usa el molde existente)

Usa el componente `LinkPortalWidget` que ya existe en `src/components/widgets/shared/LinkPortalWidget.tsx`. Crea un nuevo widget envoltorio (wrapper) y su `widgetConfig.tsx` siguiendo el patrón de los widgets del proyecto. No crees un componente genérico nuevo ni modifiques `LinkPortalWidget`.

Ahora espera mis datos y rellena todo con la informacion que te de. Si no proporciono un id, generarlo a partir del titulo (kebab-case).

Datos que te dare:
- titulo
- descripcion
- icono (archivo fuente en otra carpeta; hay que adaptarlo y copiarlo a `public/icons/` con el nombre acordado)
- url principal
- texto del boton “abrir en pestaña nueva”
- botones extra (opcionales): texto + url
- categoria del widget
- tamano por defecto (ancho, alto)

Notas adicionales del usuario (pendientes):
- 
